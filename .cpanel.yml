---
deployment:
  tasks:
    - cd /home/$USER/repositories/T2W
    - /bin/npm install --omit=dev
    - mkdir -p /home/$USER/public_html
    - /bin/cp -f .htaccess /home/$USER/public_html/.htaccess 2>/dev/null; true
    - mkdir -p /home/$USER/repositories/T2W/tmp && touch /home/$USER/repositories/T2W/tmp/restart.txt
